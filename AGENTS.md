# AgriScope 项目执行规则

默认使用简体中文。先理解完整用户流程与真实研究内容，再制定计划并修改代码。

## 产品真相

- AgriScope 是面向辽宁农业场景的**交互式数据研究产品**，不是数据大屏、BI、新闻站、论文阅读器或聊天产品。
- 核心问题：辽宁不同城市的农业与市场如何面对气象风险？哪些风险会真正传导、哪些会被市场系统吸收？
  改变天气条件或市场缓冲条件，可能发生什么？
- 结构固定为：省域（`/liaoning`）→ 城市研究空间（`/cities/:cityId`）→ 研究点（`/cities/:cityId/research/:researchId`）。
- 研究内容唯一来源是外部研究工程的索引与正文，通过 `ResearchRepository` 读取；前端不新增、不改写研究结论。
- 沈阳是当前唯一完整样板（方法模板 + 内容模板 + UI 模板 + 交互模板）；其他城市只保留入口与元数据。

## 内容边界（红线）

- 不把相关性写成因果；不把再分析天气写成气象站实测；不把成交量写成吨；
  不把年度生产数据插值成日度；不把批发市场蔬菜称作沈阳本地产；不把模型情景写成历史事实；
  不把不显著写成没有影响；不为前端效果编造阈值、数值或结论。
- 阴性结果是正式结论，必须能正面表达，不能只展示"显著发现"。
- 实验性结果（模型估计 / 情景模拟）必须与"实际观测"在视觉与文案上区分，并常驻显示模型可信边界。
- 数据真实性优先于页面美观：宁可如实标注"交互数据正在接入"或使用真实静态图。

## 体验边界

- 一个页面只负责一个任务；一个区域最多一个主操作；导航与操作分离；全局导航只保留少量入口。
- 交互研究默认模式；渐进披露：现象 → 分析 → 方法与限制，方法学名词不放在第一屏。
- 动画必须服务数据表达；问自己"删掉它用户是否更难理解数据或空间关系"，答案否定就删。
- 尊重 `prefers-reduced-motion`（退化为瞬时），保留键盘操作、焦点、移动端可用与性能降级。
- 使用暖白研究出版物体系；避免卡片墙、AI 渐变、装饰性粒子、蓝紫科技大屏。

## Ponytail 最小实现阶梯

来源：`frontedpage/design-references/ponytail`（MIT）。每次编码按顺序停在第一个可行层级：

1. 这项功能真的需要存在吗？不需要就删除或跳过。
2. 代码库里已有实现吗？复用并修正共享根因。
3. 标准库或浏览器原生能力能完成吗？优先使用（图表用 SVG 自建，不引入图表库）。
4. 已安装依赖能完成吗？不要为少量代码增加依赖。
5. 只写满足当前验收标准的最小正确实现。

删除优先于新增，复用优先于重写，根因修复优先于路径补丁。
不能简化掉数据校验、错误处理、安全、无障碍和明确要求。

## 施工约束

- 禁止运行时直接访问研究工程路径；研究内容必须经同步脚本进入 `public/research`。
- 禁止把 `data/ models/ reports/ outputs/` 全量复制进前端。
- 任何删除前先更新 `docs/ITEACH_MIGRATION_MATRIX.md`，确认旧模块已被新组件取代。
- 每个阶段完成后必须 `npm run build` 通过；新增非平凡逻辑至少保留一个能证明行为的自动化检查。
